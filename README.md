1. Compile the KV_Store
```bash
make
```

2. Start the server
```bash
make run
```

3. To run the benchmark, open a second terminal window and run the benchmark
```
python3 benchmark.py
```

4. The Benchmark should show our throughput, latency, and operations per second.
